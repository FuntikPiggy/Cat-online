server {
  listen 80;
  # index index.html;

  location /api/ {
    proxy_set_header Host $http_host;
    proxy_pass http://backend:8000/api/;
  }
  location /admin/ {
    proxy_set_header Host $http_host;
    proxy_pass http://backend:8000/admin/;
  }F:\Dev\kittygram_final\nginx\nginx.conf

  location /media/ {
    alias /media/;
    index index.html;
    #try_files $uri $uri/ /index.html;
  }

  location / {
    alias /static/;
    index index.html;
    #try_files $uri $uri/ /index.html;
  }
}
